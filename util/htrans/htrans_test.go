package htrans

import (
	"testing"
)

func TestTrans(t *testing.T) {

	/*
		var (
			lap LicenseAddParam
			db  = global.MySQLDB
		)


		if err := c.ShouldBind(&lap); err != nil {

			// 使用方法1
			ok := Trans(err)
			helpers.OutError(c, helpers.ResponseJson{Code: -1, Msg: ok})
			return

			// 使用方法2
			// tarns, _ := common.GetLocalTrans("zh") // 翻译中文
			// errs, ok := err.(validator.ValidationErrors)
			// if !ok {
			// 	helpers.OutError(c, helpers.ResponseJson{Code: -1, Msg: err.Error()})
			// } else {
			// 	m := common.RemoveTopStruct(errs.Translate(tarns))
			// 	helpers.OutSuccess(c, m)
			// }
			// return
		}

	*/

}
